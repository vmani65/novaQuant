package path.to._40c.nqCore.service;

import org.springframework.stereotype.Component;

/** The MONTHLY calendar's slot (row id=2) — the contract the LONG_MONTHLY book trades. */
@Component
public class MonthlySymbolCache extends SymbolCache {
}
