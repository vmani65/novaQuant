package path.to._40c.nqCore.service;

import org.springframework.stereotype.Component;

/** The WEEKLY calendar's slot (row id=1) — the contracts the SYNTH_WEEKLY book trades. */
@Component
public class WeeklySymbolCache extends SymbolCache {
}
