# ============================================================
# Test profile — isolated DB, never touches signals.db
# ============================================================
# Activate with:
#   java -jar nqCore-2026.05.jar --spring.profiles.active=mock,test
#
# Overrides the datasource URL so all JPA writes go to signals_test.db.
# Combined with mock profile: no real Kite calls, no production data at risk.

spring.datasource.url=jdbc:sqlite:C:/novaquant/data/sqlite/signals_test.db
